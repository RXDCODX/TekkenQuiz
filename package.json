{
  "name": "tekken-move-quiz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "node scripts/deploy-gh-pages.mjs",
    "build:db": "node scripts/build-moves-db.mjs",
    "build:db:pattern": "node scripts/build-moves-db.mjs --video-mode pattern"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  }
}
